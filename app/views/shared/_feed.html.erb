<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    <!--renderの引数は名前を気にしているわけではなく、その実体で振る舞いが決まる。Micropostモデルのインスタンスオブジェクトの集合なので、-->
    <!--_micropost.html.erbのパーシャルが繰り返し処理される。-->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>